<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.driver_v2">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PantallaRuta3"></activity>
        <activity android:name=".PantallaRuta2" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Pantallamapa3"
            android:label="@string/title_activity_pantallamapa3" />
        <activity
            android:name=".Pantallamapa2"
            android:label="@string/title_activity_pantallamapa2" />
        <activity
            android:name=".Pantallapruebamapa1"
            android:label="@string/title_activity_pantallapruebamapa1" />
        <activity
            android:name=".Pantallamapa1"
            android:label="@string/title_activity_pantallamapa1" />
        <activity android:name=".PantallaRuta1" />
        <activity android:name=".PantallaSesion" />
        <activity android:name=".PantallaCerrarSesion" />
        <activity android:name=".PantallaViajes" />
        <activity android:name=".PantallaHistorial" />
        <activity
            android:name=".MapsGM"
            android:label="@string/title_activity_maps_g_m" />
        <activity android:name=".PantallaMenu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>